{
  "name": "tensor-protocol",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "pnpm --filter @tensor/tests test"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "typescript": "^5.3.0"
  }
}
