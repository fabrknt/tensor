{
  "name": "tensor-protocol",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "cd packages/core && pnpm run build",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "anchor:build": "anchor build",
    "anchor:test": "anchor test"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "turbo": "^2.4.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@10.31.0"
}
