[package]
name = "tensor-math"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = ["anchor"]
anchor = ["tensor-types/anchor"]

[dependencies]
tensor-types = { workspace = true, default-features = false }
