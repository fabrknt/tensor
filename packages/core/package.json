{
  "name": "@tensor/core",
  "version": "0.1.0",
  "description": "Core math and logic for Tensor protocol — margin, Greeks, intent solving",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
